// components/ConversationList.js
import React from "react";

const ConversationList = ({ setSelectedConversation }) => {
  // Fetch and store conversations in state
  const conversations = [];

  return (
    <div>
      <h2 className="text-2xl font-semibold mb-4">Conversations</h2>
      {/* Render the list of conversation cards here */}
    </div>
  );
};

export default ConversationList;
